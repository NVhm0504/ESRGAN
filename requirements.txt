# core (dataset generation, metrics, TUG, statistics, tables, figures) - no GPU needed
numpy>=1.24
scipy>=1.10
pandas>=2.0
opencv-python-headless>=4.8
scikit-image>=0.21
scikit-learn>=1.3
matplotlib>=3.7
pyyaml>=6.0
# deep learning (training, inference, LPIPS, complexity); paper used PyTorch 2.1 + CUDA
torch>=2.1
torchvision>=0.16
lpips>=0.1.4
